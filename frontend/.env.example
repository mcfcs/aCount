# aCount frontend — environment variables
# Copy to frontend/.env and fill in. Vite only exposes vars prefixed with VITE_.

# Must match API_KEY in the backend .env when API-key auth is enabled.
# Leave blank for unauthenticated local development.
VITE_API_KEY=
